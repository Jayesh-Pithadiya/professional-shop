# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration (MUST BE 32+ CHARACTERS)
JWT_SECRET=your_super_secure_jwt_secret_key_minimum_32_characters_long

# Admin Credentials (CHANGE THESE!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=YourSecurePassword123!
ADMIN_EMAIL=admin@yourdomain.com

# Email Configuration (Optional)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env file to git
# 2. Use strong passwords (min 12 characters, mixed case, numbers, symbols)
# 3. Generate JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 4. Keep this file secure and never share it
